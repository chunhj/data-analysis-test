[project]
name = "data-analysis-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chunhj", email = "chunhj.kr@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
data-analysis-test = "data_analysis_test:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
